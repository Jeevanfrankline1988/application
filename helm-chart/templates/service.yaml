apiVersion: v1
kind: Service
metadata:
  name: {{ include "pygame.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "pygame.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: http
